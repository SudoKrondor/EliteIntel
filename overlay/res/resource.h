// Win32 resource ids, shared by overlay.rc and platform_win32.c so the icon is
// named once rather than as a bare number on each side of the resource boundary.

#ifndef EI_OVERLAY_RESOURCE_H
#define EI_OVERLAY_RESOURCE_H

#define IDI_APPICON 101

#endif
