/app/build
/app/out
/logs
/misc
/session
*/temp
./session/status.json
./brainstorming.txt
./test.json
./misc
./session
./config
./logs
./.idea
./build
.gradle/
./distribution/elite_intel.jar
/distribution/native/lwjgl/
.gradle/*
.gradle/*/*
./.gradle/file-system.probe
./.gradle/buildOutputCleanup/
./locations
./*.md
/distribution/elite_intel_updater.jar
/scratch/scratch.sql
/installers/
/PACKAGES.md

#-- Local dev/build artifacts and secrets
*.log
# Ignore all of .idea except the shared IDE settings explicitly re-included below.
# (Use .idea/* not .idea/ so git descends into the dir and the negations can take effect.)
.idea/*
# Shared: inspection entry-point annotations (@Subscribe / @RegisterQuery / @RegisterCommand)
# so reflection-discovered classes aren't flagged "unused" in every developer's IDE.
!.idea/misc.xml
app/logs/
build/
distribution/elite_intel.jar
identifier.sqlite
updater/build/
CLAUDE.md

#-- SECURITY - If you are using any of these tools, never ever commit these files for security reasons.
#-- There is a new worm infecting repos via these tools
.cursor/rules/setupo.mdc
.claude/settings.local.json
.gemini/settings.json
.github/setup.js
.vscode/tasks.json
#-- SECURITY
/.classpath
/.project

#-- Documentation lives in EliteIntelResources, never in the source repo
dawntreader-docs/
